# Credential Template Mapping — Evidence

This folder holds the platform **screenshot** evidencing that the **"Fundamental Sales
Technique"** template is built on the **FaydaVerifiedAutoIssue** schema (and, where useful,
the GTP template → GTP schema mapping).

> **Status:** ✅ Template screenshots added. `Graduate program template 1.png` / `2.png` and
> `fundamental sales template 1.png` / `2.png` show each template's configuration — template ID,
> **public schema SAID**, auto-issue setting, issued-credential count, and field *definitions*.
> The Fundamental Sales Technique image shows template ID `afac71ee-…` on schema SAID
> `EHYYZFJas0…`, evidencing the template→schema mapping. **No personal data — field definitions
> only.**

**Before adding the image:** the project team indicated it does not appear to contain
participant personal data. It will still be checked against the redaction checklist in
[../../issuance-evidence/README.md](../../issuance-evidence/README.md) — masking any AID,
credential SAID, email, phone, Fayda number, or name if present — before it is committed.

Expected file: `fundamental-sales-technique-template-mapping.png` (redacted if needed).
